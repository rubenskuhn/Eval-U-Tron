// // import { ChakraProvider } from "@chakra-ui/react";
// // import * as React from "react";
// import dbConnect from "../../../../db/connect";
// import question from "../../../../db/model/question";

//     const db = rubenskuhn.db(eval-u-tron);
//     const collection = db.collection(questions);

//     // Find documents
//     collection.find({}).toArray((err, docs) => {
//       if (err) {
//         console.error('Error occurred while fetching documents', err);
//         client.close();
//         return;
//       }

//       // Calculate average
//       const sum = docs.reduce((acc, doc) => acc + doc.fieldName, 0);
//       const average = sum / docs.length;
//       console.log('Average:', average);

//       client.close();
//     });
//   });
